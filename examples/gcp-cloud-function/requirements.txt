# ---------------------------------------------------------------------------
# Install the vm_power_manager library directly from the public GitHub repo.
# If you fork this project, replace `Tarunrj99` below with your GitHub handle.
#
# For production, pin to a tag (e.g. @v1.1.0) or a commit SHA so deploys are
# reproducible. `@main` is convenient for quick iteration only.
# ---------------------------------------------------------------------------
vm-power-manager @ git+https://github.com/Tarunrj99/vm-power-manager.git@v1.5.0

# Cloud Functions 2nd gen runtime requires this:
functions-framework>=3.5.0
